# NOTE: this test requires mkdocs-macros in order to run
# You must install it, if not done
# `pip install mkdocs-macros`

site_name: Hello World
theme: mkdocs

nav:
    - Home: index.md
    - Next page: second.md

plugins:
  - search
  - macros
  - test

extra:
  apples: 10
  unit_cost: 50

